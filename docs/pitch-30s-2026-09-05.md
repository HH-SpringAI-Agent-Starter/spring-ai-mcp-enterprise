# 30 秒话术包（JD 定向）— 2026-09-05

> 用途：Upwork proposal / LinkedIn 首屏 / 简历项目描述三段式。
> 原则：**先给工程叙事，再给可验证证据（仓库链接 + 测试/文档），最后给安全审查话术。**

---

## 1. Commerzbank — Development Java Lead (MCP) ｜银行级 MCP 网关

**Opening：**

> Java Lead with production MCP gateway experience: built spring-ai-mcp-enterprise (0→V1.20, 20 releases, 17-module green build) — an enterprise MCP server covering RBAC, OAuth2 (RFC 6750/7009/9700), rate limiting, audit, multi-tenant isolation and an MCP+A2A dual-protocol gateway with signed Agent Cards.

**安全段落：**

> For regulated environments: token-level authorization is enforced per tool call (insufficient_scope/403), every action is audit-logged, and A2A agent cards are JWS-signed (HS256) with constant-time verification — designed to survive a bank's security review.

**证据：** github.com/HH-SpringAI-Agent-Starter/spring-ai-mcp-enterprise ｜ docs/security-review-checklist.md（D3 证据链）

## 2. NTT DATA — MCP and Enterprise Integration Engineer ｜"auth that survives a security review"

**Opening：**

> MCP server + tool contract + JSON Schema + OAuth2/OIDC + authorization checks: this is exactly the feature set of my open-source project, now at V1.20.

**逐条对照（面试/回答用）：**

| NTT DATA 关键词 | 项目对应 |
| --- | --- |
| authorization checks | ToolScopePolicy fail-closed（V1.19）|
| least-privilege controls | 工具级 Scope ACL：requiredScopes 声明 + scope 通配 |
| RBAC/ABAC | RBAC + 多租户隔离（row/schema/instance 三档）|
| tool contract / JSON Schema | 工具注册中心 + 契约校验 |
| observability | Prometheus 指标 + 审计端点 |

**证据：** docs/security-review-checklist.md（B2/B3/D 全套）+ scope 演示脚本 `examples/scope-demo.sh`

## 3. Greelow — MCP Developer（$6-9K/月 远程）｜OAuth 2.1 + per-user scoping

**Opening：**

> OAuth 2.1 + per-user scoping + rate limits + audit logs — implemented and demonstrable: my MCP enterprise server issues OAuth2 client-credentials tokens (RFC 6750), maps token scopes down to per-tool ACLs, enforces per-tenant quotas and keeps end-to-end audit trails.

**加分点：** 三语言客户端示例（Java/Node/Python）+ Docker/k8s + GitHub Actions 矩阵 = 交付物齐全，远程协作零摩擦。

**证据：** examples/client-java、examples/curl-examples.sh、docs/oauth2-guide.md

## 4. 沃尔玛中国 — 高级AI平台/应用开发工程师（¥30-55K/月）｜AI/MCP 网关

**Opening（中文）：**

> AI/MCP 网关 + MCP Server + 多 MaaS 统一接入 + 鉴权/限流/熔断/灰度：我开源的 spring-ai-mcp-enterprise 已完整实现企业级 MCP 网关（RBAC、OAuth2、限流、审计、多租户、工具级 Scope 权限），并有完整中文文档与 RFC 中文解读（6750/7009/9700）。

**加分项对照：** Nacos/etcd 注册中心经验在项目文档中体现；Token 计费与成本归集可基于审计日志扩展；Agent 编排（RAG/工具调用）见 mcp-examples/dify 与 mcp-client-spring-ai 示例。

## 5. Upwork 通用 proposal 模板（200 字）

> Hi — I built an open-source enterprise MCP server (spring-ai-mcp-enterprise, 20 releases, 17 modules, all tests green) covering exactly what production MCP work needs: OAuth2 client-credentials + refresh/revoke, per-tool scope ACLs (least privilege), rate limiting, multi-tenant isolation (row/schema/instance), audit logging, and an MCP+A2A dual-protocol gateway with signed agent cards. It ships with Java/Node/Python client examples, Docker/k8s deployment, GitHub Actions CI and Chinese+English docs, so handoff is frictionless. For this job's MCP server build, I can start with a scoped spike in week 1 (auth + tool contract) and iterate from there. Repository: github.com/HH-SpringAI-Agent-Starter/spring-ai-mcp-enterprise

---

## 简历项目描述（一段式）

> **spring-ai-mcp-enterprise（开源，V1.20）** — 企业级 MCP Server 框架，17 模块、20 个版本：RBAC 安全、OAuth2 全生命周期（Client Credentials/Refresh/吊销/jti 防重放，RFC 6750/7009/9700）、网关限流与按租户配额、三级审计日志、多租户三档隔离（Row/Schema/Instance + 生命周期管理 API）、工具级 Scope 权限映射（Token Scope → Tool ACL，fail-closed）、MCP+A2A 双协议网关（SSE 流式 + JWS Signed Agent Card + Bearer 强制鉴权）。配套 GitHub Actions（JDK17/21 矩阵）、Docker/k8s、三语言客户端示例与中文文档。