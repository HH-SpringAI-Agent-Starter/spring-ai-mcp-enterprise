# MCPize manifest - https://mcpize.com
# Generated by mcpize analyze
version: 1
name: spring-ai-mcp-enterprise
description: >-
  Enterprise-grade MCP Server framework built with Java 17 and Spring Boot 3.4, enabling AI Agents to securely access
  database queries, web searches, and system monitoring tools.
runtime: container
entry: mcp-server/target/*.jar
build:
  install: 'mvn dependency:go-offline'
  command: mvn clean package -DskipTests
startCommand:
  type: http
  command: java
  args:
    - '-jar'
    - app.jar
